name: Org Release
on:
    pull_request:
        types:
        - closed
        branches:
        - main

jobs:
  Create-Release:
    uses: Coalfire-CF/Actions/.github/workflows/org-release.yml@v0.1.0